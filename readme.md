# Terraform Templates

This repository contains reusable Terraform learning templates:

- EC2 with IAM Role + SSM
- S3 with versioning and encryption
- VPC basic networking

Each template is self-contained inside templates/.